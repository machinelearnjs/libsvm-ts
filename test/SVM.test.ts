import { SVM } from "../src/SVM";
/*
describe('SVM', () => {
  let svm: SVM;

  beforeEach(() => {
    svm = new SVM({
      options: {
        type: 'C_SVC',
        kernel: 'LINEAR',
        epsilon: 0.001,
        quiet: false,
        probabilityEstimates: true
      }
    });
  });

  describe('.train(...)', () => {
    it('should train using XOR data', () => {

    });
  });
});
*/
